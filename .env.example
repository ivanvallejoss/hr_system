# =============================================================================
# DJANGO CONFIGURATION
# =============================================================================

# SECURITY WARNING: keep the secret key used in production secret!
# Generate a new one with: python -c 'from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())'
SECRET_KEY=django-insecure-CHANGE-THIS-IN-PRODUCTION

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG=True

# Comma-separated list of allowed hosts
ALLOWED_HOSTS=localhost,127.0.0.1


# =============================================================================
# DATABASE CONFIGURATION (PostgreSQL)
# =============================================================================

DB_NAME=hr_system
DB_USER=postgres
DB_PASSWORD=your_secure_password_here
DB_HOST=localhost
DB_PORT=5432


# =============================================================================
# DEPLOYMENT NOTES
# =============================================================================

# For Railway/Render deployment, the DATABASE_URL will be auto-generated.
# You can override the individual DB_* variables above, or use:
# DATABASE_URL=postgresql://user:password@host:port/dbname

# For production, remember to:
# 1. Set DEBUG=False
# 2. Generate a new SECRET_KEY
# 3. Update ALLOWED_HOSTS with your domain
# 4. Use a strong DB_PASSWORD